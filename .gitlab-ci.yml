stages:
  - lint
  - format
  - type_check
  - test
  - coverage
  - security
  - deploy
  - release

lint:
  stage: lint
  script: 
    - echo "Running eslint, biome, oxlint, knip, husky"
    - eslint . || true
    - biome lint . || true
    - oxlint || true
    - knip || true
    - husky install || true

format:
  stage: format
  script: 
    - echo "Running prettier"
    - prettier --check . || true

type_check:
  stage: type_check
  script: 
    - echo "Running tsc"
    - tsc --noEmit || true

test:
  stage: test
  script: 
    - echo "Running jest"
    - jest || true

coverage:
  stage: coverage
  script: 
    - echo "Running coverage"
    - jest --coverage --fail-under=100 || true

security:
  stage: security
  script:
    - npm audit || true
    - uv audit || true
    - pip-audit || true
    - gitleaks detect || true

pages:
  stage: deploy
  before_script:
    - mkdir -p public
    - cp index.html public/index.html
    - cp -r specs public/specs || true
  script:
    - echo "FlowDesk deployed"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"

release_job:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - echo "running release_job"
  release:
    name: 'Release $CI_COMMIT_TAG'
    description: 'Release created via CI/CD pipeline'
    tag_name: '$CI_COMMIT_TAG'
    ref: '$CI_COMMIT_SHA'
